name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize]
    # Only activate for specific labels or after Copilot Agent completes
    branches: [main]

jobs:
  claude-review:
    # Disable temporarily while Copilot Agent works
    if: false  # Disabled until Copilot Agent completes fixes
    
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write  # Changed from read to write
      issues: write       # Added write permission
      id-token: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code Review
        id: claude-review
        uses: anthropics/claude-code-action@beta
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}  # Added GitHub token
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          
          # Configuración en español para revisiones de código
          custom_instructions: |
            Eres Claude, revisor de código especializado en el proyecto Forestech Colombia.
            
            IMPORTANTE: Realiza TODA la revisión en español.
            
            Contexto del proyecto:
            - Monorepo React + Vite + Firebase para gestión forestal
            - Aplicaciones: alimentacion/ (alimentación bovina) y combustibles/ (gestión de combustibles)
            - Stack tecnológico: React, JavaScript, Firebase, ESLint, Vite
            
            Criterios de revisión:
            1. 🔍 Funcionalidad: Verifica que el código funcione correctamente
            2. 📝 Calidad: Revisa patrones, estructuras y mejores prácticas
            3. 🚫 Errores: Detecta bugs potenciales y problemas de linting
            4. 🔄 Mantenibilidad: Evalúa legibilidad y sostenibilidad del código
            5. ⚡ Rendimiento: Identifica optimizaciones posibles
            
            Formato de comentarios:
            - Usa emojis para categorizar (🔍 🐛 ⚡ 📝 ✅)
            - Proporciona ejemplos de código cuando sea necesario
            - Explica el "por qué" detrás de cada sugerencia
            - Mantén un tono constructivo y educativo
            
            Enfoque especial en:
            - Hooks de React y ciclo de vida
            - Manejo de estado y props
            - Integración con Firebase
            - Patrones de componentes reutilizables
            
          # Direct prompt para revisión automática (no necesita @claude)
          direct_prompt: |
            Revisa este pull request y proporciona retroalimentación en español sobre:
            - Calidad del código y mejores prácticas
            - Posibles bugs o problemas
            - Consideraciones de rendimiento
            - Problemas de seguridad
            - Cobertura de pruebas
            
          allowed_tools: "Bash(npm run lint:*),Bash(npm run build:*),Bash(npm run test:*)"
            
            Be constructive and helpful in your feedback.
          
          # Optional: Customize review based on file types
          # direct_prompt: |
          #   Review this PR focusing on:
          #   - For TypeScript files: Type safety and proper interface usage
          #   - For API endpoints: Security, input validation, and error handling
          #   - For React components: Performance, accessibility, and best practices
          #   - For tests: Coverage, edge cases, and test quality
          
          # Optional: Different prompts for different authors
          # direct_prompt: |
          #   ${{ github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR' && 
          #   'Welcome! Please review this PR from a first-time contributor. Be encouraging and provide detailed explanations for any suggestions.' ||
          #   'Please provide a thorough code review focusing on our coding standards and best practices.' }}
          
          # Optional: Add specific tools for running tests or linting
          # allowed_tools: "Bash(npm run test),Bash(npm run lint),Bash(npm run typecheck)"
          
          # Optional: Skip review for certain conditions
          # if: |
          #   !contains(github.event.pull_request.title, '[skip-review]') &&
          #   !contains(github.event.pull_request.title, '[WIP]')

