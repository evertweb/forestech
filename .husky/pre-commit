#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🌲 Verificando landing antes del commit..."

npm run build || {
  echo "❌ La verificación de la landing falló. Revisa public/index.html."
  exit 1
}

echo "✅ Landing verificada. Continuando con el commit."
