// Duplicate legacy test kept to preserve history. Skipped to avoid JSX parsing in .js.
import { describe, it } from 'vitest';
describe.skip('BaseModal (legacy js test)', () => {
  it('skipped', () => {});
});
